# نفس أصل الطلبات المعتمَد في public/client/*.ts
# VITE_API_BASE_URL: قاعدة واحدة فقط (مثلاً /api مع Vite proxy، أو https://host/marketplace/api/v1).
# لا تضف /api مرة ثانية في مسارات getData/postData — يُجمَع تلقائياً مع apiBaseUrl.

# مؤقتًا أثناء ربط الواجهة بالباكند — لا تُفرَض شاشة login
