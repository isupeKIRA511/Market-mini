/**
 * مرجع يدوي يطابق VITE_API_BASE_URL في `.env` (انظر `.env.example`).
 * الواجهة الفعلية تستخدم `src/lib/config/features.ts` و `src/lib/api/client.ts`.
 */
export const baseURL = 'http://localhost:5051';
